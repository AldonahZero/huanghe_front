import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // Re-enable ESLint during builds to keep the project healthy.
    // If the build fails due to many lint errors, we'll fix them incrementally.
};

export default nextConfig;
